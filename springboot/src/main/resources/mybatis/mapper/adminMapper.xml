<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.main.ateam.member.dao.*">


<select id="memberList" resultType="memberVO"> <!-- 회원 리스트 -->
	select num,id,name,age,gender,tel,email,addr,mdate from member order by 1 desc
</select>
<select id="hospitalList" resultType="memberVO"> <!-- 병원 - 업체 (1:1 관계) -->
	select h.hnum,h.hname,h.hloc,h.otime,
	h.ctime,h.hcate,h.hurl,h.hemail,h.hgrade,
	c.crn,c.cate,c.cdate from hospital h,cate c 
	where h.cnum=c.cnum order by 1 desc
</select>
  	
</mapper>